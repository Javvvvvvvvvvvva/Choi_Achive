{
  "name": "threejs-journey-exercise",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "cp -r src/model.obj dist/ && cp -r src/textures dist/ && cp -r src/audio dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.16",
    "vite-plugin-restart": "^0.4.1"
  },
  "dependencies": {
    "lil-gui": "^0.19.2",
    "three": "^0.166.1"
  }
}
